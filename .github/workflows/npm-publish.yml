name: Publish to npm

on:
  release:
    types: [created]

jobs:
  publish:
    name: Publish @studio packages
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'
          registry-url: https://registry.npmjs.org/

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build all packages
        run: pnpm build

      - name: Run tests
        run: pnpm test

      - name: Publish packages (dependency order)
        run: |
          pnpm --filter @studio/contracts publish --access public --no-git-checks
          pnpm --filter @studio/anonymizer publish --access public --no-git-checks
          pnpm --filter @studio/ralph publish --access public --no-git-checks
          pnpm --filter @studio/runner publish --access public --no-git-checks
          pnpm --filter @studio/engine publish --access public --no-git-checks
          pnpm --filter @studio/api publish --access public --no-git-checks
          pnpm --filter @studio/cli publish --access public --no-git-checks
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

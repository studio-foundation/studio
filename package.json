{
  "name": "studio",
  "version": "0.3.0",
  "private": true,
  "description": "Studio v7 — Agentic pipeline orchestrator (monorepo)",
  "license": "AGPL-3.0-only",
  "author": "Ariane Guay",
  "repository": {
    "type": "git",
    "url": "https://github.com/studio-foundation/studio.git"
  },
  "scripts": {
    "build": "pnpm --filter @studio/contracts build && pnpm --filter @studio/anonymizer build && pnpm --filter @studio/ralph build && pnpm --filter @studio/runner build && pnpm --filter @studio/engine build && pnpm --filter @studio/api build && pnpm --filter @studio/cli build",
    "clean": "pnpm -r run clean",
    "test": "pnpm -r run test"
  }
}

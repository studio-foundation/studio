# Studio v7 — Environment Variables

# Required: at least one LLM provider API key
ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_API_KEY=sk-...

# Directory where repos are cloned for pipeline runs
STUDIO_PROJECTS_DIR=/path/to/studio-projects

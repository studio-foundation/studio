name: recipe-critique
version: 1
schema:
  required_fields:
    - status
    - feedback
    - issues
post_validation:
  rejection_detection:
    field: status
    approved_values:
      - approved
    details_field: issues
    summary_field: feedback
    reject_if_non_empty: issues

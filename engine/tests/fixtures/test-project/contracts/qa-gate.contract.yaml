
name: qa-gate
version: 1
schema:
  required_fields:
    - status
    - issues
post_validation:
  rejection_detection:
    field: status
    approved_values:
      - approved
      - pass
    details_field: issues
    summary_field: summary

name: code-generation
version: 1
schema:
  required_fields:
    - summary
    - files_changed
tool_calls:
  minimum: 1
  required_tool_groups:
    - [repo_manager-write_file, repo_manager-apply_patch]

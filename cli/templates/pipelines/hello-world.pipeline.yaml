name: hello-world
description: Simple test pipeline — one stage, no tools
version: 1

stages:
  - name: greet
    kind: custom
    agent: analyst
    ralph:
      max_attempts: 1
    context:
      include:
        - input
